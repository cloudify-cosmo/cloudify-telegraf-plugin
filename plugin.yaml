plugins:
  telegraf:
    executor: host_agent
    source: https://codeload.github.com/cloudify-cosmo/cloudify-telegraf-plugin/zip/first
    package_name: cloudify-telegraf-plugin
    package_version: '0.1'